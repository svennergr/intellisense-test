/**
 * Class to hold a whole component.
 *
 * @export
 * @class Main
 */
export default class Main {
  /**
   * Creates an instance of Main.
   * @param {Component} component The component to assign.
   * @memberof Main
   */
  constructor(component) {
    this.assignedComponent = component;
  }
}
